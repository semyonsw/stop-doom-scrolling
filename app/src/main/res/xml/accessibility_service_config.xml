<?xml version="1.0" encoding="utf-8"?>
<!--
  flagReportViewIds is the critical one: without it every node's
  viewIdResourceName comes back null and all view-id matching silently
  fails while looking like a detection bug.

  No android:packageNames filter on purpose - the same service has to see
  the social apps and the browsers.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="am.onex.stopdoom.ui.MainActivity" />
